import React from "react";
import classes from './Toolbar.module.css'

const Toolbar = () => {

    return (
        <div className={classes.Toolbar}>Burger Builder App</div>
    )
}

export default Toolbar